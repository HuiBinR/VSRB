code usage:
1. Use code in "lucene" to split the mention file, build index file and retrieval it.

data usage:
1. Train: the data extracted from Gigaword corpus.
2. Test: the ACE-R2 data.

note：
1. The sep file and the training set are too big to upload, so we will share the link to download it.